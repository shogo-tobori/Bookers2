<%= form_for(@profile) do |f| %>
<%= f.text_field :name, autofocus: true %>
<%= attachment_image_tag @profile, :profile_image, :fill,60, 60 , fallback: "no_image.jpg" %>
<%= f.attachment_field :profile_image, placeholder:"プロフィール画像"%>
<%= f.submit "変更を保存"　%>
<% end %>